[phases.setup]
nixPkgs = ["ffmpeg", "python311"]

[phases.install]
cmds = ["python -m pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "python patch_bot.py && python -m py_compile main.py quiz.py config.py supabase_sync.py && python main.py"
