[phases.setup]
nixPkgs = ["ffmpeg", "python311", "noto-fonts"]

[phases.install]
cmds = ["python -m pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "python patch_bot.py && python archive_patch.py && python -m py_compile main.py quiz.py config.py supabase_sync.py archive_sync.py question_archive_worker.py && python main.py"
