name: Apply RATHOD VIP Bot Upgrade
on:
  workflow_dispatch:
  push:
    paths:
      - 'vip_question_engine.py'
      - 'persistent_scores.py'
      - 'app_update_notifier.py'
      - 'vip_commands.py'
      - 'vip_scheduler.py'
      - 'multi_provider.py'
      - 'premium_hub.py'
      - 'apply_vip_bot.py'
      - '.github/workflows/apply-vip-bot.yml'
permissions:
  contents: write
jobs:
  upgrade:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Wire and validate all VIP modules
        run: |
          python apply_vip_bot.py
          python -m py_compile main.py quiz.py vip_question_engine.py persistent_scores.py app_update_notifier.py vip_commands.py vip_scheduler.py multi_provider.py premium_hub.py apply_vip_bot.py
          grep -q 'premium_hub.init_commands' main.py
          grep -q 'CommandHandler("hub"' main.py
          grep -q 'CallbackQueryHandler' main.py
      - name: Commit generated wiring
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add main.py *.py
          git diff --cached --quiet && exit 0
          git commit -m "Wire premium Telegram command center [skip ci]"
          git push
